# Codex 跨设备开发交接

更新时间：2026-09-14 18:00（Asia/Shanghai）

项目：`NBA--2008` / 篮坛传奇：重返2008

活动 ID：`app_a1c57bc9c2`

当前开发分支：`codex/random-trade-mode`
交接前代码提交：`d7c4c97 feat: simulate full overtime periods`

## 接手后先做

```powershell
git fetch origin
git switch codex/random-trade-mode
git pull origin codex/random-trade-mode
npm ci
npm run lint
npm test
npm run build
npm run dev
```

- 下一版本同时包含经典模式和新模式“平行联盟”，后续开发继续在 `codex/random-trade-mode` 进行。
- `master` 当前停在 `efea626`，是当前线上经典模式维护分支。不要把新模式分支反向整体合并到 `master`。
- 不要提交 `credentials.json`、`.agents/skills/runbook/state/app_a1c57bc9c2.json` 等设备凭据或本地状态。
- 通用的比赛、属性、荣誉、退役、存档与移动端修改必须同时覆盖经典模式和平行联盟；交易、球队方向和命定事件等玩法才按模式分流。
- 每个模式只有一个生涯存档。不要恢复旧的四存档入口或 `slot_2/3/4` 逻辑。

## 2026-09-14 从上午至今完成的工作

### 1. 属性溢出换算 GOAT

- 综评达到当前上限且剩余属性点无法使用时，页面不再继续显示大量属性点，改为显示对应 GOAT 奖励和下一档进度。
- 换算规则维持原计算：每 100 点未使用属性点计入 10 点 GOAT；以前的记录也一直按此规则计算，本次主要补充可视化。
- 溢出状态下属性广告按钮置灰。
- 重置属性后若重新出现可加点空间，会退出溢出状态并允许重新分配，不能借此突破年龄综评上限。
- 经典模式和平行联盟均使用同一逻辑。

### 2. 经典模式更新同步与单存档结构

- 将 master 中 v6、v7、v8 的必要通用修复同步进新模式分支，并检查经典模式流程没有被平行联盟逻辑污染。
- 两个模式分别保留一个生涯存档和一个个人传奇榜命名空间，互不覆盖。
- 旧的经典模式存档没有 `gameMode` 时仍按经典模式读取，升级不会清空现有玩家存档。
- 删除无入口的多存档页面和旧存档槽调用，保留兼容旧数据所需的最小迁移代码。

### 3. 平行联盟玩法说明

- 首页入口改为明显的“新模式：玩法说明”按钮，并按要求移除按钮前图标。
- 玩法说明改成经典模式/平行联盟左右对比，详细说明球队方向查看位置、赛季交易、动态选秀和球星邀请入口。
- 球星邀请说明不再写“看广告”。
- 玩法说明和创建球员长页面在移动端加入常驻滚动条提示。

### 4. 创建球员与前两个选秀赛季

- 检查并测试创建球员“综评+10”不会绕过18项属性上限或破坏属性溢出逻辑。
- 心仪球队选择已前置，减少玩家忽略后一直被湖人选中的情况。
- 已删除“请先选择球队，选秀大会上该球队将动用专属签位选中你”提示。
- 平行联盟的 2008、2009 两届新秀按历史真实顺位进入联盟；从 2010 届开始再按上赛季战绩动态选秀。

### 5. 平行联盟命定事件

- 新增数十个真实历史背景的命定事件，覆盖决定一、决定二、杜兰特的抉择、灯泡组合、詹姆斯下一站、浓眉加盟湖人、布鲁克林组合等时间线。
- 所有触发条件公开显示，包括必须条件、球队方向、胜场、阵容人数、前序事件和指定分支等数字条件。
- 事件只在指定赛季触发；未到赛季不可操作，过期后显示失效。
- 多个重要事件提供分支选择，并在页面明确显示各分支结果。
- 触发涉及名单变化的事件后，核心球员获得 2 至 3 年随机交易保护。
- 决定一继续要求詹姆斯此前未夺冠；仅对平行联盟早期骑士加入轻微模拟战力修正，降低 2009 年过度夺冠概率。
- 已建立前序依赖：决定二依赖决定一；灯泡组合依赖“大胡子独当一面”；浓眉加盟湖人依赖“詹姆斯的下一站”的指定分支；布鲁克林三巨头依赖杜兰特与欧文先到篮网等。
- 已删除停摆缩水赛季、三分浪潮、封闭园区和湖人 F4 事件。
- 命定事件入口是平行联盟核心入口；打开时会像设置一样暂停常规赛模拟。
- 列表默认展示前 5 个，其余可展开；展开后的移动端滚动条常驻。
- 详情页使用固定标题、可滚动正文、固定底部按钮的三段式布局；完成事件后只展示结果，不再重复条件和分支。
- 修复事件列表弹窗空白：主容器必须有明确的 `h-[94dvh]`，不能只有 `max-height`。

### 6. 比赛、最佳阵容与广告公共修复

- 调高并统一亲自上场绝杀概率公式，高关键得分属性会得到明确更高成功率。
- 最佳阵容评选加入球队战绩、球员综评和全面数据权重；60胜的25+5+5、70胜的24+3+15高综评核心均有自动化覆盖。
- 检查属性广告奖励：完整播放后一次性发放 30 属性点，并限制生涯次数；两个模式共用。
- 自由市场和赛季交易报价均改为 3 支球队；广告刷新一次后按钮禁用。
- 移动端创建页面、玩法说明、命定事件等长页面保持滚动条可见。

### 7. v8 公告与传奇榜详情

- v8 公告将“新模式——平行联盟”单独做高亮模块，简述动态交易、球队方向、选秀归属、命定事件和球星邀请。
- 按用户要求删除公告开头两句模式概述，并删除“修复广告奖励偶尔未发放”一项。
- 公告继续明确：旧版本未使用属性点也已经换算 GOAT，本次只是把换算过程显示出来。
- 平行模式个人传奇榜卡片右上角显示“平行时空”标识，不单独占一行。
- 平行模式退役记录会保存本生涯解锁的命定事件、赛季、分支和结果；本地榜与全网榜详情均能展示。此前已经退役的旧记录无法补回当时未保存的命定事件快照。

### 8. 完整加时赛（最新）

- 旧逻辑会在第四节平局后直接补随机分差，现在已删除该捷径。
- 经典模式和平行联盟、常规赛和季后赛的“亲自上场”均支持完整加时赛。
- 第四节平局后进入真实 NBA 时长的 5:00 加时；加时仍平会继续第二个、第三个及后续 5:00 加时，直到分出胜负。
- 加时比分、比赛日志、球员得分和上场时间走原比赛事件队列并累计进最终战报。
- 本地 `localhost` 的比赛控制区临时显示“调试：第四节平局”，可直接跳到第四节结束 96:96。该按钮按用户要求仅用于测试，正式上线前必须删除。
- 已实际验证 UI：第四节 96:96 → “进入加时赛” → `OT1 05:00` → 加时日志与最终战报。

## 全网传奇榜：当前最重要的发布阻断项

### 本地代码已经完成

- 前端 `src/lib/globalLeaderboard.ts` 的读取、我的排名、上传和待补报键均携带 `gameMode`。
- 本地云函数 `activity/cloudfunctions/activity_api/index.js` 已支持 `classic` 与 `random_trade` 参数。
- 数据库迁移：
  - `20260914180000_split_leaderboard_by_game_mode.sql`
  - `20260914190000_read_leaderboard_by_game_mode.sql`
- 设计目标是两个模式各自按 PUID 保留最高记录、各自前 50 名，不允许混榜；缺省旧请求永远等同于经典模式。
- 平行模式上传的记录包含 `gameMode: random_trade` 和已解锁命定事件快照。

### 线上仍在运行旧云函数

- 今天曾意外执行过一次部署脚本：数据库迁移 `20260914180000` 已应用，给现有记录补了默认 `game_mode = classic` 并建立模式索引；现有经典榜数据没有被删除。
- 该次操作没有成功更新线上 `activity_api` 云函数。之后已停止所有云端写操作。
- 2026-09-14 17:45 左右只读复核：
  - `/api/health` 返回 `code: 0`。
  - 旧 `/api/leaderboard` 返回 50 条。
  - `?gameMode=classic` 返回 50 条。
  - `?gameMode=random_trade` 也返回同样 50 条。
  - 两个模式的记录 ID 顺序完全一致，榜首均为 `d3d75b81-67c3-4566-aea2-6764653d186a`。
- 结论：当前线上经典榜正常且未受新代码影响；线上平行榜尚未启用。当前不能直接发布新前端，否则平行榜会读到经典榜，旧写接口也无法保证模式隔离。

### 正式发布顺序

1. 先使用 `.agents/skills/act-cloudbase/SKILL.md` 和官方 CloudBase MCP 核对活动环境 `app_a1c57bc9c2`。
2. 应用尚未上线的模式读取迁移 `20260914190000`，确认 `20260914180000` 已存在且不要重复破坏数据。
3. 更新 `activity_api` 云函数代码和现有网关路由，不新建重复环境或重复函数。
4. 部署后验证：
   - 无参数旧 URL 与 `gameMode=classic` 结果完全一致。
   - `random_trade` 只返回平行记录，初始为空也属正常。
   - 两个模式可以由同一 PUID 分别保存最高记录。
   - 匿名读取可用，未登录写入返回 401/403。
   - 经典榜原有 50 条及榜首记录不变。
5. 云端全部验证通过后，再发布包含平行联盟的新前端包。

部署辅助脚本是 `activity/cloudbase-mcp-deploy.cjs`。它包含迁移、函数、路由和隔离验证，但运行会修改线上服务，执行前必须再次确认目标环境和发布窗口。不要在只想检查状态时运行完整 `all` 阶段。

## 当前验证结果

- `npm run lint`：通过。
- `npm test`：93/93 通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- 本地视觉验证：创建球员滚动条、玩法说明滚动条、命定事件列表与详情、个人传奇榜模式标识、第四节平局及 5 分钟加时均已检查。
- 测试覆盖经典旧存档兼容、双模式单存档隔离、双模式个人榜隔离、平行榜请求参数、命定事件依赖/分支/交易保护、前两届真实选秀、长期联盟模拟、最佳阵容、广告属性奖励和加时比分/时长。

## 接下来建议按此顺序继续

1. 在本地继续测试加时赛，重点手测季后赛、双加时和高上场时间球员的个人数据累计。
2. 用户确认本地调试完成后，删除“调试：第四节平局”按钮及对应跳转函数。
3. 做一次经典模式从创建、选秀、常规赛、季后赛、休赛期到退役的回归抽查。
4. 做一次平行联盟命定事件、随机交易、退役、个人榜的完整抽查。
5. 按上面的顺序完成 CloudBase 双模式排行榜部署与只读/写入验证。
6. 最后重新生成上传包并做虎扑发布安全审查，再发布 v8。

## 今日主要提交

- `6a9f459` 属性溢出 GOAT 奖励
- `f40e8df`、`4bd958e` 同步 master 更新
- `b7316fd` 每个模式仅一个生涯存档
- `7569859` 至 `59d0dca` 平行联盟玩法说明
- `7b9c8af` 创建球员综评加成属性上限测试
- `6e199e1` 至 `8aa7d83` 命定事件、分支、依赖与平衡
- `9e0b3a0` 前两届历史真实选秀顺位
- `c0c5e55` 删除湖人 F4 命定事件
- `f6659a4` 比赛、最佳阵容、广告和心仪球队修复
- `4be3f34` 创建页移动端滚动条
- `1d9ab45` 平行联盟 v8 发布准备与双模式榜代码
- `c37eb49` 公告高亮、榜单标识、命定事件退役快照和弹窗修复
- `d7c4c97` 完整 5 分钟连续加时赛
