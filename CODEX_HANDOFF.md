# Codex 跨设备开发交接

更新时间：2026-09-14

项目目录：`NBA--2008`

活动 ID：`app_a1c57bc9c2`

## 开始前先做

1. 切换并更新经典模式主分支：`git switch master`、`git pull origin master`。
2. 安装依赖并启动：`npm ci`、`npm run dev`。
3. 修改前先运行：`npm run lint`、`npm test`、`npm run build`。
4. `.agents/skills/runbook/state/app_a1c57bc9c2.json` 是设备本地状态，不要提交。
5. 除非用户明确要求，不要自行提交或推送 Git。

## 分支状态

- `master`：经典历史模式，也是当前线上版本的维护分支。本交接文档所在提交包含 v7 稳定性更新。
- `codex/random-trade-mode`：平行联盟新模式开发分支，远端已有提交 `f3b1cf0 feat: 完善平行联盟长期模拟流程`，目前暂停开发。
- 经典模式已经上线，稳定性优先。不要随意把新模式分支整体合并进 `master`；恢复新模式开发前，应先按用户要求同步主分支的必要修复。

## 双模式维护原则

- 当前新版本在 `codex/random-trade-mode` 上继续开发，发布包同时包含经典历史模式与平行时空模式。
- 属性、比赛、季后赛、退役、存档、移动端适配和性能等通用修改必须同时检查两个模式；只有选秀、交易、球队策略等明确属于玩法差异的逻辑才按模式分流。
- 每次合并或新增通用功能后，必须确认经典模式原有流程没有退化，并运行完整 TypeScript、测试与生产构建检查。
- 每个模式只保留一个生涯存档；首页继续生涯读取当前模式的唯一存档。个人传奇榜默认跟随当前游戏模式，两个模式的数据保持隔离。

## 本次 v7 已完成内容

### 未来新秀

- 2026 年加入本地新秀数据；2027 年及以后改用固定种子的本地生成名单。
- 共用 60 个名字与 60 个姓氏组合，避免后期大量重复姓名和反复出现“杰森·塔图姆”。
- 未来选秀名单会真正进入联盟阵容，未来最佳新秀评选也使用同一生成逻辑。
- 已验证连续 25 个赛季、750 名新秀无姓名重复。

### 属性加点与老将上限

- 老将达到综评上限后，允许重置并重新分配已返还的点数，不允许借此突破年龄上限。
- 手机端恢复显示“老将效应：当前总评封顶上限”。
- 新增独立的属性重分配逻辑与自动化测试。

### 页面闪屏与跳转

- 移除阶段切换中的异步 `startTransition` 包装，改为同步批量更新页面状态。
- 按顺序预热主要懒加载页面，降低选秀、季后赛、休赛期及新赛季切换时的白屏概率。
- “更多”导航不再在 `pointerdown` 阶段提前打开，避免移动端触摸穿透后误跳模式首页。

### 退役流程

- 常规赛结束离场时不再错误弹出年龄退役提示。
- 42 岁球员完成休赛期并点击进入下赛季后，年龄变为 43 岁，但不会进入常规赛或生成新赛季；会直接显示强制退役提示。
- 点击“正式退役”后关闭提示并进入退役结算。
- 强制退役提示出现时不再同时在背景弹出常规赛荣誉弹窗。

### 季后赛存档隔离

- 修复不同生涯按赛季年份复用季后赛缓存，导致例如 2015-2016 赛季错误继承热火总冠军的问题。
- 新建球员会生成唯一生涯 ID；季后赛缓存加入 `careerSignature` 并校验球员、生涯和球队。
- 旧的、已完成但无法确认归属的季后赛冠军缓存会被拒绝；未完成的旧存档可继续，并在下次保存时补写签名。

### 公告

- 上述内容已写入游戏内 v7 公告，包括未来新秀、属性重置、移动端年龄上限、闪屏优化、退役流程、季后赛缓存和“更多”导航修复。

## 已完成验证

- `npm run lint`：通过。
- `npm test`：58/58 通过。
- `npm run build`：通过。
- `git diff --check`：通过，仅有 Windows CRLF 提示。
- 使用当前真实存档做过页面检查：首页、继续生涯、“更多”、生涯时间线和赛季页跳转正常，未修改玩家存档内容。
- 当前真实存档角色只有 21 岁，因此 43 岁退役流程主要由代码审查和自动化测试覆盖，没有篡改真实存档强行触发。

## 后续建议

- 若继续经典模式维护：先从 `master` 开始，重点收集仍可复现的 WebView 闪屏设备、系统、发生页面和操作路径，不要在没有复现证据时继续大范围修改渲染结构。
- 若继续平行联盟：切换 `codex/random-trade-mode`，先核对与 `master` 的差异，再只同步必要的经典模式修复。新模式已有独立存档、随机交易、球队发展方向、未来选秀及长期联盟模拟基础。
- 平行联盟全网榜在首个版本中暂时锁定；个人榜与经典模式分开。不要把两种模式的 GOAT 数据混入同一排行榜。
